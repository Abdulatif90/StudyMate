# Web framework
fastapi>=0.115
uvicorn[standard]>=0.30

# Settings / config
pydantic-settings>=2.5
python-dotenv>=1.0

# Database (Neon Postgres) — SQLModel wraps SQLAlchemy; psycopg2 is the driver
sqlmodel>=0.0.22
psycopg2-binary>=2.9
alembic>=1.13

# Auth (Clerk) — JWT verification via JWKS
pyjwt[crypto]>=2.9

# Document upload + text extraction (Documents module — PDF/DOCX/TXT)
python-multipart>=0.0.12
pypdf>=5.0
python-docx>=1.1

# Embeddings (Cohere) + pgvector column type
cohere>=5.0
pgvector>=0.3

# Generation (Claude) — Ask endpoint
anthropic>=0.110

# Async jobs — move document parse/chunk/embed off the request path
inngest>=0.5

# File storage (Cloudflare R2, S3-compatible) — app/core/r2_client.py
boto3>=1.34

# Billing (Polar, Merchant of Record) — app/core/polar_client.py. Brings in
# `standardwebhooks`, which backs the webhook signature verification.
polar-sdk>=0.31
